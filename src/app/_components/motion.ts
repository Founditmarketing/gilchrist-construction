/** Gilchrist signature easing — a confident, weighty settle (mirrors the CSS
 *  curve in gilchrist.css). Single-sourced so Hero / Reveal can't drift apart. */
export const GC_EASE = [0.16, 1, 0.3, 1] as const;
